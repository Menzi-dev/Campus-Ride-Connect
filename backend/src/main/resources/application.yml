spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/campus_connect?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    username: root
    password: s1gwebela
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  address: 0.0.0.0

jwt:
  secret: your-very-secure-secret-key-change-in-production
  expiration: 3600000

logging:
  level:
    com.campusconnect: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG